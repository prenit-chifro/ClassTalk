source 'http://rubygems.org'

# To solve ssl errors
	gem 'certified'

# Bundle edge Rails instead: gem 'rails', github: 'rails/rails'
	gem 'rails', '~> 5.0.0', '>= 5.0.0.1'

# Use mysql as the database for Active Record
	gem 'mysql2', '>= 0.3.18', '< 0.5'

# Use Puma as the app server
	gem 'puma', '~> 3.0'

# Use SCSS for stylesheets
	gem 'sass-rails', '~> 5.0'

# Use Uglifier as compressor for JavaScript assets
	gem 'uglifier', '>= 1.3.0'

# Use CoffeeScript for .coffee assets and views
	gem 'coffee-rails', '~> 4.2'

# See https://github.com/rails/execjs#readme for more supported runtimes
	gem 'therubyracer', platforms: :ruby

# Use jquery as the JavaScript library
	gem 'jquery-rails'

# Turbolinks makes navigating your web application faster. Read more: https://github.com/turbolinks/turbolinks
	gem 'turbolinks', '~> 5'

# Build JSON APIs with ease. Read more: https://github.com/rails/jbuilder
	gem 'jbuilder', '~> 2.5'

# Use Redis adapter to run Action Cable in production
	gem 'redis', '~> 3.0'

# Use ActiveModel has_secure_password
	gem 'bcrypt', '~> 3.1.7'

# Use Capistrano for deployment
	gem 'capistrano-rails', group: :development

group :development, :test do
	# Call 'byebug' anywhere in the code to stop execution and get a debugger console
		gem 'byebug', platform: :mri
end

group :development do
	# Access an IRB console on exception pages or by using <%= console %> anywhere in the code.
		gem 'web-console'
end

# Windows does not include zoneinfo files, so bundle the tzinfo-data gem
	gem 'tzinfo-data', platforms: [:mingw, :mswin, :x64_mingw, :jruby]


# For JQuery UI 
	gem 'jquery-ui-rails'
	
# For User Account Authentication
	gem 'devise'

# For Enabling OmniAuth with Devise
	gem 'omniauth'
	
# For Facebook Authentication with Devise Gem	
	gem 'omniauth-facebook'
	
# For Google Authentication with Devise Gem
	gem 'omniauth-google-oauth2'
	
# For Paperclip File Attachment
	gem 'paperclip'	

# For video processing
	gem 'paperclip-av-transcoder'

# For playing videos
	gem 'videojs_rails'

# For Ajax File Upload	
	gem 'remotipart'

# For GCM Notifications on android
	gem 'rpush' 

# For Web Notification  Service Worker
	gem "webpush"
	
# For Calculating map distances	
	gem 'geocoder'
	
# For Pagination
	gem 'kaminari'

# For Material Icons
	gem 'material_icons'
	
# For Background tasks
	gem 'sidekiq'
		
# For Social Sharae links
	gem 'social-share-button', '~> 0.1.6'

# For Calendar
	
	gem 'fullcalendar-rails'
	gem 'momentjs-rails'

# For Safari push notification package generation
	gem 'push_package'	

# For firefox calendar and font
	gem 'bootstrap3-datetimepicker-rails', '~> 4.14.30'
	gem 'font-awesome-rails'

# For Bootstrap
	gem 'bootstrap-sass', '~> 3.3.6'
	
# For JQuery UI
	gem 'jquery-ui-rails'
